# TODO

- Generate `shared/protocol.d.mts` from a typed protocol source instead of maintaining it by hand alongside `shared/protocol.mjs`. The runtime and declaration now carry sync comments, but there is still no compiler-enforced relationship between them.
